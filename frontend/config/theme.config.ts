// Umi `theme` option (Less variables). Unused for runtime theming — the active
// design-token layer is the Ant v5 ThemeConfig in `src/theme/tokens.ts`, wired
// through ConfigProvider in `src/app.tsx`. Kept as the umi convention entry.
const themeConfig = () => {
  return {};
};

export default themeConfig;
