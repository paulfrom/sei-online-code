---
owner: project
origin: platform_seed
memorySeedTemplateCode: default
agentMemorySeedVersion: 1
reviewStatus: unreviewed
lastReviewedAt:
---

# Modules

> 本文件由平台 seed 初始化生成（origin: platform_seed）。
> 维护模块边界和集成点。概览设计依据本文件做"新增/复用/调整模块"映射，详细设计围绕本文件界定的模块展开。

## Module Boundaries

模块划分原则与边界约束，规定哪些职责属于哪个模块、哪些不得跨模块调用。

- 待项目维护：列出模块边界规则。

## Existing Modules

当前代码中已存在的模块清单。生成设计时复用/扩展必须以这里为准，不得声称复用不存在的模块。

- 待项目维护：列出现有模块及其职责。

## Planned Modules

计划新增但尚未实现的模块。生成设计时区分"新增"与"复用已有"。

- 待项目维护：列出计划新增模块。

## Integration Points

模块间及系统间的集成点，包括接口、事件、共享数据、外部依赖。生成设计时必须显式列出涉及的集成点。

- 待项目维护：列出集成点。